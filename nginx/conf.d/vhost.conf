server {
    listen 443 ssl;

    server_name  ~^(?<subdomain>.+)\.yanayarosh\.ru$;

    error_log /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log combined if=$loggable;

    root /var/www/app/consumer/web;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # deny accessing php files for the /assets directory
    location ~ ^/assets/.*\.php$ {
        deny all;
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_script_name;
    }

    ssl_certificate      /etc/ssl/yanayarosh.ru/fullchain.pem;
    ssl_certificate_key  /etc/ssl/yanayarosh.ru/privkey.pem;
}

server {
    listen 443 ssl;

    server_name  login.yanayarosh.ru;

    error_log /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log combined if=$loggable;

    root /var/www/app/login/web;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # deny accessing php files for the /assets directory
    location ~ ^/assets/.*\.php$ {
        deny all;
    }

    location ~ \.php$ {
        try_files $uri =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass php:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param PATH_INFO $fastcgi_script_name;
    }

    ssl_certificate      /etc/ssl/yanayarosh.ru/fullchain.pem;
    ssl_certificate_key  /etc/ssl/yanayarosh.ru/privkey.pem;
}